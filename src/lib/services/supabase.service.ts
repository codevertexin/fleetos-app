/**
 * Supabase Service — Future Integration Placeholder
 *
 * When ready to connect Supabase:
 * 1. Install: npm install @supabase/supabase-js
 * 2. Add env vars: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY
 * 3. Uncomment the client initialization below
 * 4. Replace mock-data imports across the app with real Supabase queries
 */

// import { createClient } from '@supabase/supabase-js';

// const supabaseUrl = import.meta.env.VITE_SUPABASE_URL as string;
// const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY as string;

// export const supabase = createClient(supabaseUrl, supabaseAnonKey);

export const supabasePlaceholder = {
  ready: false,
  message: 'Supabase integration pending. Configure VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.',
};
