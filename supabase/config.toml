# FleetOS Supabase — development project only
project_id = "kjiwzqysjassakvrojun"

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323

[db.migrations]
enabled = true

# Auth Core JWT is not a Supabase JWT — Edge verifies Auth Core separately (Phase 6).
[functions.fleetos-sync-identity]
verify_jwt = false

[functions.fleetos-list-tenants]
verify_jwt = false
